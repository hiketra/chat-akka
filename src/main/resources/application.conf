service {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
  neo4j {
    user = "neo4j"
    password = "example"
    host = "bolt://0.0.0.0:7687"
  }

  files = "firehol_level1.netset,firehol_level4.netset"
  files = ${?FILES_TO_SCRAPE} #system env variable that can be overridden
}
